# Server configuration
PORT=7777
LEVEL_SEED=1337

# Client configuration (consumed by Vite)
# Set VITE_SERVER_PORT= (explicitly empty) to connect to the same origin the
# page was served from — used by the production Docker build.
VITE_SERVER_ORIGIN=ws://localhost:7777
VITE_SERVER_PATH=/
